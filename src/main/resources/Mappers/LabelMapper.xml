<!DOCTYPE mapper>
<mapper namespace="LabelMapper">
    <!--  -->
    <select id="selectLableName" parameterType="pojo.po.Label" resultType="pojo.po.Label">
        select  labelName from label where labelId=#{labelId}
    </select>

    <select id="selectLableByLableId" resultType="pojo.po.Label">
        select labelId,labelName from lable where labelId = #{labelId}
    </select>>


</mapper>